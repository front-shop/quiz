import { styled } from '@mui/material';

const ProgressWrap = styled('div')(() => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: '400px',
  width: '100%'
}));

export { ProgressWrap };
